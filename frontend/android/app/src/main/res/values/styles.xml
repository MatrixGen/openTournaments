<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <!-- Required by Capacitor -->
    <style name="Theme.App" parent="AppTheme" />

    <!-- No Action Bar -->
    <style name="Theme.App.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!-- Splash / Launch theme -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

</resources>
