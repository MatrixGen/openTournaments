import { registerPlugin } from '@capacitor/core';

// The name 'ScreenRecorder' MUST match @CapacitorPlugin(name = "ScreenRecorder") in Java
export const ScreenRecorder = registerPlugin('ScreenRecorder');